def init_global(puzzle, total_size_):
    """
    init some global variables
    """
    global resolved_puzzle
    global total_size
    resolved_puzzle = puzzle  # the resolved puzzle
    total_size = total_size_  # the total size (size*size
